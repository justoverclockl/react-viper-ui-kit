import { ReactNode } from 'react';

export interface CardContentWrapperProps {
    /**
     * The content to be displayed inside the card wrapper.
     * Accepts any valid ReactNode (text, elements, or components).
     */
    children: ReactNode
}
