export type UseWindowSizeStateType = {
    /**
     * The current width of the browser window in pixels.
     * This value dynamically updates as the window is resized.
     */
    width: number

    /**
     * The current height of the browser window in pixels.
     * This value dynamically updates as the window is resized.
     */
    height: number
};
