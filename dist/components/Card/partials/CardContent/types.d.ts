import { ReactNode } from 'react';
export interface CardContentProps {
    /**
     * The content to be displayed inside the card.
     * Accepts any valid ReactNode (text, elements, or components).
     */
    children: ReactNode;
}
